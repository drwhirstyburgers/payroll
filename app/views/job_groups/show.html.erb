<%= render 'layouts/partials/card_form' %>
<div class="row">
    <div class="col-md-6">
        <h1 class="display-4">Group Name: <%= @job_group.name %></h1>
    </div>
    <div class="col-md-6">
        <%= link_to "Edit", edit_job_group_path(@job_group), class: "btn btn-dark float-right" %>
    </div>
  </div>

<h4>
  <strong>Wage:</strong>
  $<%= @job_group.wage %>/hour
</h4>

<%= render 'layouts/partials/card_form_base' %>
